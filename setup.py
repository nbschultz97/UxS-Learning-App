from setuptools import setup, find_packages

setup(
    name="uxs-learning-app",
    version="0.1.0",
    description="Offline UxS learning CLI",
    packages=find_packages(),
    python_requires=">=3.9",
)
